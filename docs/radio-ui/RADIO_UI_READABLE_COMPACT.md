# Radio UI - Читаемый и компактный дизайн

## Проблемы

1. **Текст не читается** - шрифты были слишком мелкие, элементы маленькие
2. **Блоки занимают весь экран** - нужно чтобы занимали только половину экрана
3. **Баг переключения** - при первом клике на RADIO плеер не пропадает, только после второго клика

## Решения

### 1. Увеличены размеры элементов плейлиста для читаемости

#### Margins и Spacing
- Margins: `8, 6, 8, 6` → `12, 8, 12, 8`
- Spacing: `10` → `12`

#### Порядковый номер
- Width: `30px` → `35px`
- Font size: `14px` → `16px`

#### Обложка альбома
- Размер: `40x40` → `50x50`

#### Song info layout
- Spacing: `2px` → `3px`

#### Текст (увеличен для читаемости)
- **Label** (PREVIOUS/NOW PLAYING/NEXT):
  - Font size: `10px` → `11px`

- **Title**:
  - Font size: `13px` → `15px`
  - Max width: `500px` → `600px`

- **Artist**:
  - Font size: `11px` → `13px`
  - Max width: `500px` → `600px`

- **Duration**:
  - Width: `45px` → `50px`
  - Font size: `12px` → `14px`

#### Высота элемента
- `46px` → `60px`

### 2. Уменьшена левая панель (плеер) для компактности

Цель: оба блока (плеер + плейлист) должны занимать половину экрана

#### Контейнер
- Width: `280px` → `200px`
- Margins: `12, 6, 12, 6` → `10, 5, 10, 5`
- Spacing: `4` → `3`

#### Album Art
- Размер: `100x100` → `80x80`

#### Текст
- Artist: `9px` → `8px`
- Song Title: `11px` → `10px`
- Time labels: `8px` → `7px`

#### Прогресс-бар
- Spacing: `4` → `3`

#### Кнопки управления
- Play/Pause: `28x28` → `24x24`
- Border radius: `14px` → `12px`
- Volume button: `20x20` → `18x18`
- Volume button font: `11px` → `10px`
- Volume slider width: `60px` → `50px`
- Control spacing: `6px` → `4px`
- Between spacing: `6px` → `4px`

#### Кнопка REQUEST SONG
- Padding: `6px 12px` → `4px 8px`
- Font size: `9px` → `8px`

### 3. Исправлен баг переключения на радио

**Проблема**: При клике на кнопку "📻 EKNM Intercom" (eknmIntercomBtn) плеер не скрывался, потому что вызывался только `showRadio()`, который не переключал режим.

**Решение**: Добавлена проверка в `showRadio()`:

```cpp
void MainWindow::showRadio()
{
    // If we're not in RADIO mode yet, switch to it first
    if (currentMode != RADIO) {
        switchToRadioMode();
        return;
    }

    stackedWidget->setCurrentWidget(radioPage);
    eknmIntercomBtn->setProperty("active", true);

    // Load radio background
    radioPage->loadRadioBackground();

    // Refresh styles
    eknmIntercomBtn->style()->unpolish(eknmIntercomBtn);
    eknmIntercomBtn->style()->polish(eknmIntercomBtn);
}
```

**Логика**:
- Если текущий режим НЕ RADIO → вызываем `switchToRadioMode()` который:
  - Скрывает плеер SONGS
  - Показывает кнопки RADIO навигации
  - Вызывает `showRadio()` снова (уже в правильном режиме)
- Если уже в режиме RADIO → просто показываем радио-страницу

## Визуальный результат

### Левая панель (Player) - компактная
```
┌────────────┐
│  [80x80]   │  ← Обложка меньше
│  Album Art │
│            │
│ Artist (8) │  ← Шрифты меньше
│ Title (10) │
│ 0:00 ─ 0:00│  ← Все компактно
│            │
│  [▶] 🔊 ── │  ← Кнопки меньше
│            │
│ [REQUEST]  │  ← Маленькая кнопка
└────────────┘
  200px wide
```

### Правая панель (Playlist) - читаемая
```
┌────────────────────────────────────────┐
│            PLAYLIST                    │
├───┬─────┬─────────────────────┬───────┤
│ 1 │ [50]│ PREVIOUS (11px)     │ 3:21 │  ← 60px высота
│   │     │ Song Title (15px)   │      │
│   │     │ Artist Name (13px)  │      │
├───┼─────┼─────────────────────┼───────┤
│ 2 │ [50]│ NOW PLAYING (синий) │ 5:07 │  ← 60px высота
│   │     │ Current Song (15px) │      │
│   │     │ Current Artist (13) │      │
├───┼─────┼─────────────────────┼───────┤
│ 3 │ [50]│ NEXT (11px)         │ 2:54 │  ← 60px высота
│   │     │ Next Song (15px)    │      │
│   │     │ Next Artist (13px)  │      │
└───┴─────┴─────────────────────┴───────┘

Высота элемента: 60px
Обложка: 50x50px
Шрифты: КРУПНЫЕ и ЧИТАЕМЫЕ!
```

### Общий размер
- **Левая панель**: 200px
- **Правая панель**: занимает оставшееся место
- **Вместе**: занимают примерно половину экрана (зависит от размера окна)

## Файлы изменены

### client/src/ui/baseradiopage.cpp

**setupLeftPanel()** - компактная панель:
- Width: 200px
- Album art: 80x80
- Все шрифты уменьшены
- Все отступы/spacing уменьшены
- Кнопки меньше

**addSongToList()** - читаемые элементы:
- Margins: 12, 8
- Spacing: 12
- Order number: 35px, 16px
- Thumbnail: 50x50
- Song info spacing: 3px
- Title: 15px (увеличен!)
- Artist: 13px (увеличен!)
- Duration: 14px (увеличен!)
- Height: 60px (увеличена!)

**updateBackgroundImage()** - обновлен размер обложки:
- `scaled(80, 80)`

### client/src/ui/mainwindow.cpp

**showRadio()** - исправлен баг переключения:
```cpp
// If we're not in RADIO mode yet, switch to it first
if (currentMode != RADIO) {
    switchToRadioMode();
    return;
}
```

## Тестирование

1. Откройте приложение
2. Нажмите на кнопку RADIO (переключатель)
3. Проверьте:
   - ✅ Плеер SONGS сразу пропадает (после первого клика!)
   - ✅ Левая панель 200px широкая, компактная
   - ✅ Обложка 80x80px
   - ✅ Элементы плейлиста 60px высотой
   - ✅ Текст хорошо читается (крупные шрифты!)
   - ✅ Оба блока занимают примерно половину экрана

4. Нажмите на "📻 EKNM Intercom"
5. Проверьте:
   - ✅ Плеер SONGS пропадает (если был в режиме SONGS)
   - ✅ Радио-страница открывается

## Преимущества

1. **Читаемость**: крупные шрифты (15px title, 13px artist, 14px duration)
2. **Компактность**: левая панель 200px → оба блока занимают половину экрана
3. **Фиксед баг**: плеер SONGS скрывается при первом клике на радио
4. **Удобство**: все элементы хорошо видны и легко кликаются

## Размеры до/после

### Плейлист элементы:
| Элемент | Было | Стало |
|---------|------|-------|
| Height | 46px | 60px (+30%) |
| Thumbnail | 40x40 | 50x50 (+25%) |
| Title font | 13px | 15px (+15%) |
| Artist font | 11px | 13px (+18%) |
| Duration font | 12px | 14px (+17%) |

### Левая панель:
| Элемент | Было | Стало |
|---------|------|-------|
| Width | 280px | 200px (-29%) |
| Album art | 100x100 | 80x80 (-20%) |
| All fonts | - | Уменьшены на 1-2px |

**Итог**: Плейлист БОЛЕЕ читаемый, плеер БОЛЕЕ компактный!
